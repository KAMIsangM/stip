# 智能互动教学平台项目工作日记

## 项目基本信息
- **项目名称**：智能互动教学平台（Smart Interactive Teaching Platform）
- **项目周期**：2026年06月01日 - 2026年06月27日（计20个工作日）
- **开发人员**：任庚辰（华中科技大学软件学院软件工程2303班）
- **项目类型**：大学生实训项目
- **开发模式**：单人全栈开发（前端+后端+文档）

---

## 第一阶段：项目立项与需求分析（6月1日-6月4日）

### 2026年6月1日（星期一） - 项目启动与立项报告编制

**工作内容**：
今天是智能互动教学平台项目的第一天。作为华中科技大学软件学院的一名本科生，我开始了这个基于多智能体技术的互动教学系统开发实训项目。上午我仔细研究了项目的背景和意义，明确了项目的核心目标：构建一个能够通过集成大语言模型、语音合成、语音识别等多模态能力，将传统单向授课转变为沉浸式互动学习体验的教学平台。

在项目启动阶段，我进行了详细的技术调研，了解了当前智能教育领域的发展现状和存在的问题。发现传统的在线教育平台大多以视频播放和文本阅读为主，缺乏互动性和个性化。而我的项目要通过AI教师与AI同学的实时互动，创造接近真实课堂的学习体验。

下午我主要完成了项目立项报告（Project ID_INIT_002）的编制工作。立项报告中详细阐述了项目的核心功能模块，包括智能课堂生成、多模态教学内容生成引擎、课程知识库与知识图谱管理、语音功能集成等。同时对项目的工作量进行了初步估算，预计总工作量为25人天。我还明确了项目的系统边界，确定项目将聚焦课程知识库管理、知识图谱构建、多模态教学内容自动生成等核心功能，而不涉及底层大语言模型的自主研发。

**完成情况**：
- 完成项目启动和技术调研
- 完成项目立项报告编制（约3000字）
- 明确项目目标和系统边界
- 初步估算项目工作量

**明日计划**：
- 对立项报告进行自我评审和完善
- 开始软件需求规格说明书的编制
- 进行详细的需求调研和分析

---

### 2026年6月2日（星期二） - 立项报告完善与需求分析启动

**工作内容**：
今天上午我对立项报告进行了仔细的审查和完善。作为单人开发项目，我需要同时考虑技术可行性和时间成本。在审查过程中，我发现立项报告中有些地方描述不够详细，于是进行了以下补充：1）在项目简介中更明确地说明了平台支持的AI提供商（DeepSeek、通义千问、Kimi、MiniMax等）；2）在项目目标中补充了关于双导师协同教学模式的详细说明，解释了"真人教师+虚拟AI教师"如何协同工作；3）在系统边界中明确了平台不涉及底层大语言模型的自主研发，而是专注于教学内容的生成、管理与学习分析能力。

下午我启动了需求分析工作。首先，我通过查阅相关学术文献了解智能教育领域的研究现状，重点关注多模态教学内容生成、知识图谱构建、语音交互等关键技术。然后，我对市面上现有的在线教育平台（如Coursera、edX、中国大学MOOC等）进行了竞品分析，梳理出它们的主要功能点和不足之处，从而为我的平台找到差异化的功能定位。

通过文献研究和竞品分析，我梳理出了平台需要具备的核心功能点，包括：用户输入学习主题后自动生成课程、支持多种模态的教学内容（文本、PPT、音频、思维导图等）、知识图谱的可视化展示与编辑、语音合成与识别功能等。

**完成情况**：
- 完成立项报告的完善和修改
- 完成技术调研和竞品分析
- 启动需求分析工作，梳理核心功能点
- 明确项目的技术路线

**明日计划**：
- 开始软件需求规格说明书的编制
- 详细分析四大核心功能模块的需求
- 定义非功能性需求

---

### 2026年6月3日（星期三） - 软件需求规格说明书编制

**工作内容**：
今天我全天用于编制软件需求规格说明书（Project ID_Proc_RA_002）。按照GB/T 8567-2006计算机软件文档编制规范的要求，我完成了文档的主体部分，包括引言、总体描述、详细需求等章节。

在详细需求部分，我详细描述了四大核心功能模块的需求：
1. **智能课堂生成（F001）**：支持用户输入学习主题或选择预置课程，系统自动分析并生成结构化课程大纲，每个大纲条目自动编排为互动教学场景。我设计了主题输入的接口规范，规划了课程大纲的数据结构，并定义了教学场景的类型（讲解、讨论、测验、实验等）。

2. **多模态教学内容生成引擎（F002）**：支持沉浸式文本、测验、PPT与旁白、音频课程、思维导图、互动式教材等六种模态。我详细定义了每种模态的内容规范、生成流程和存储格式。例如，PPT生成需要包含标题、内容、备注，并与语音旁白文件关联存储。

3. **课程知识库与知识图谱管理（F003）**：支持知识图谱的构建、存储、可视化展示与编辑。我设计了知识图谱的数据模型，包括知识点节点（包含ID、名称、描述、类型等属性）和知识点关系（包含前驱后继、包含、因果等多元关系类型）。

4. **语音功能集成（F-Voice）**：支持TTS语音合成和ASR语音识别。我定义了语音功能的接口规范，包括语音合成的音色选择、语速调节，以及语音识别的实时流式传输协议。

同时，我还定义了非功能性需求，包括性能需求（音频生成<30秒、PPT生成<60秒、知识图谱加载<3秒）、可用性需求（界面简洁直观、操作流程清晰）、安全性需求（用户输入验证、API访问控制）等。

**完成情况**：
- 完成软件需求规格说明书初稿（约5000字）
- 明确MVP阶段（P0优先级）的功能范围
- 定义四大核心功能模块的详细需求
- 定义非功能性需求指标

**明日计划**：
- 对需求规格说明书进行自我评审
- 根据评审结果进行修改完善
- 开始概要设计工作

---

### 2026年6月4日（星期四） - 需求评审与概要设计启动

**工作内容**：
今天上午我对软件需求规格说明书进行了自我评审。作为单人开发项目，我需要站在评审人的角度，仔细检查需求的完整性、一致性和可实现性。在评审过程中，我发现需求规格说明书基本覆盖了MVP阶段的核心功能，但需要在以下方面进行补充：1）增加关于知识图谱多元关系类型的详细说明，包括关系的定义、属性和可视化展示方式；2）补充关于多智能体协同课堂的交互流程描述，说明AI教师和AI同学如何分工协作；3）明确语音功能的fallback策略，当主服务提供商不可用时如何自动切换到备用服务。

下午我根据评审意见对需求规格说明书进行了修订，并确认了需求的最终版本。同时，我启动了概要设计工作，开始规划系统的总体架构。考虑到我需要独立完成前后端开发，我选择了一套适合单人开发且效率较高的技术栈：前端使用Vue 3 + TypeScript + Vite，后端使用FastAPI + SQLAlchemy + Pydantic，数据库使用SQLite（MVP阶段）。这种技术组合具有良好的开发体验和丰富的生态支持，能够帮助我快速实现项目功能。

在架构设计方面，我采用了前后端分离的架构模式，前端通过RESTful API与后端通信。这种设计不仅使前后端开发可以并行进行，也便于后续的功能扩展和维护。

**完成情况**：
- 完成需求规格说明书的自我评审和修改
- 确认需求规格说明书最终版本
- 启动概要设计工作，确定技术架构方向
- 选择适合单人开发的技术栈

**明日计划**：
- 继续概要设计工作
- 完成系统架构的详细设计
- 开始数据库设计

---

## 第二阶段：系统设计与技术选型（6月8日-6月12日）

### 2026年6月8日（星期一） - 概要设计文档编制

**工作内容**：
今天我重新开始工作（6月5-7日为周末）。利用周末时间，我对项目的整体方案进行了深入思考，进一步明确了设计的细节。上午我继续概要设计工作，完成了概要设计文档（HLD）的引言部分，包括文档目的、软件功能范围和参考资料。在文档目的中，我明确说明本文档旨在对智能互动教学平台进行概要设计，明确系统的总体架构、模块划分、接口设计、数据库设计及部署方案。

在软件功能范围部分，我详细描述了MVP阶段需要设计的四大核心功能模块：智能课堂生成（F001）、多模态教学内容生成引擎（F002）、课程知识库与知识图谱管理（F003）以及语音功能集成（F-Voice）。针对每个功能模块，我概述了其核心功能和与其他模块的交互关系。

下午我完成了第0层设计描述，定义了软件系统的上下文关系。我明确了系统与外部实体的交互：1）用户（学生/教育工作者）通过Web浏览器访问平台，输入学习主题，获取生成的课程内容并进行互动学习；2）LLM服务（DeepSeek/通义千问等）通过RESTful API提供课程大纲生成、多模态内容创作、知识点抽取等AI能力；3）TTS服务（Edge TTS/阿里云/腾讯云）通过RESTful API提供文本转语音能力；4）ASR服务（阿里云/腾讯云）通过WebSocket提供实时语音识别能力。

同时，我开始设计系统的总体架构，采用前后端分离的架构模式。前端负责UI展示和用户交互，后端负责业务逻辑和AI服务调用。我绘制了系统的架构图，明确了各层之间的依赖关系和接口规范。

**完成情况**：
- 完成概要设计文档引言部分
- 完成第0层设计描述，明确系统上下文
- 绘制系统总体架构图
- 明确系统与外部实体的接口规范

**明日计划**：
- 完成系统架构的详细设计
- 确定技术栈和第三方服务提供商
- 开始数据库表结构设计

---

### 2026年6月9日（星期二） - 系统架构设计与技术锁定

**工作内容**：
今天上午我完成了系统架构的详细设计。经过深入的技术调研和反复比较，我确定了以下技术栈：
- **前端**：Vue 3 + TypeScript + Vite。Vue 3的响应式特性和组件化开发模式非常适合构建交互复杂的单页应用，TypeScript提供了静态类型检查，能够在开发阶段发现潜在错误，Vite则提供了极速的开发服务器启动和热模块替换能力。
- **后端**：FastAPI + SQLAlchemy + Pydantic。FastAPI基于Python 3.7+的类型提示，自动生成API文档，性能优异；SQLAlchemy是成熟的ORM框架，能够简化数据库操作；Pydantic提供了数据验证和序列化功能。
- **数据库**：SQLite（MVP阶段）。SQLite是一个轻量级的嵌入式数据库，无需独立的数据库服务器，适合单机部署的场景。
- **LLM接入**：DeepSeek（主）+ 通义千问（备用）。DeepSeek在代码理解和长文本生成方面表现出色，通义千问则在中文理解方面有优势。
- **TTS**：Edge TTS（主）+ 阿里云（备用）。Edge TTS是微软提供的免费TTS服务，音质自然，阿里云TTS则提供了更多音色选择。
- **ASR**：阿里云（主）+ 腾讯云（备用）。阿里云ASR在中文识别准确率方面表现优秀，支持实时流式识别。

下午我编写了技术文档TECH-001（数据模型与技术锁定），解决了需求规格说明书与概要设计之间的冲突，明确了数据模型的设计原则：1）使用ORM实体类作为数据模型的主要表现形式；2）优先使用Python内置类型；3）枚举值使用Python Enum。同时，我开始设计数据库表结构，初步规划了6张核心表：用户表（users）、课程表（courses）、知识点表（knowledge_nodes）、知识关系表（knowledge_relations）、生成内容表（generated_contents）、学习记录表（learning_records）。

**完成情况**：
- 完成系统架构的详细设计
- 确定技术栈和第三方服务提供商策略
- 完成TECH-001技术文档
- 初步规划数据库表结构

**明日计划**：
- 完成数据库表的详细设计
- 定义表的字段、类型、约束和索引
- 开始API接口的规划设计

---

### 2026年6月10日（星期三） - 数据库设计与API规划

**工作内容**：
今天我全天用于数据库详细设计和API接口规划。按照需求规格说明书的要求，我完成了以下数据库表的设计：

1. **courses表**：存储课程基本信息。字段包括：id（主键）、title（课程标题）、description（课程描述）、outline_json（课程大纲的JSON数据）、created_at（创建时间）、updated_at（更新时间）。我为title字段创建了索引，以提高查询效率。

2. **knowledge_nodes表**：存储知识点。字段包括：id（主键）、name（知识点名称）、description（知识点描述）、type（知识点类型：概念、原理、方法等）、course_id（所属课程的外键）、created_at（创建时间）。我为course_id字段创建了外键约束，并创建了索引。

3. **knowledge_relations表**：存储知识点关系。字段包括：id（主键）、source_node_id（源节点ID）、target_node_id（目标节点ID）、relation_type（关系类型：前驱后继、包含、因果等）、created_at（创建时间）。我为source_node_id和target_node_id创建了外键约束和索引。

4. **outlines表**：存储课程大纲条目。字段包括：id（主键）、course_id（所属课程的外键）、title（大纲条目标题）、content（条目内容）、order_index（排序索引）、created_at（创建时间）。我为course_id和order_index创建了索引，以确保大纲条目的顺序正确。

5. **generated_contents表**：存储生成的内容。字段包括：id（主键）、course_id（所属课程的外键）、content_type（内容类型：文本、PPT、音频、思维导图等）、file_path（文件路径）、metadata_json（元数据JSON）、created_at（创建时间）。我为course_id和content_type创建了索引。

6. **learning_records表**：存储学习记录。字段包括：id（主键）、course_id（所属课程的外键）、progress（学习进度）、score（测验得分）、created_at（创建时间）、updated_at（更新时间）。我为course_id创建了索引。

每个表我都明确了字段名称、类型、约束和索引。同时，我开始规划RESTful API接口，按照功能模块划分了API分组：
- `/api/courses`：课程管理接口（创建、查询、更新、删除课程）
- `/api/knowledge`：知识图谱管理接口（知识点和关系的CRUD）
- `/api/generate`：内容生成接口（生成大纲、文本、PPT、音频等）
- `/api/voice`：语音功能接口（TTS合成、ASR识别）

下午我解决了关于PPT渲染方案的技术决策。经过调研，我决定使用python-pptx库在后端生成PPTX文件，前端使用PPTX.js进行渲染。这种方案能够实现PPT的完整生成和精确渲染，同时支持语音旁白的集成。我编写了TECH-002文档（依赖与PPT渲染方案），详细记录了技术决策的过程和依据。

**完成情况**：
- 完成数据库详细设计（6张表，包括字段、类型、约束、索引）
- 完成API接口的初步规划，定义API分组和接口清单
- 完成TECH-002技术文档
- 明确PPT渲染的技术方案

**明日计划**：
- 完成概要设计文档的剩余部分
- 设计模块划分、接口规范、安全设计
- 开始搭建项目框架

---

### 2026年6月11日（星期四） - 概要设计完成与框架搭建准备

**工作内容**：
今天上午我完成了概要设计文档的剩余部分。在模块划分章节，我明确了前后端的职责划分：前端负责UI展示和用户交互，包括课程管理页面、知识图谱可视化页面、内容生成页面、语音交互页面等；后端负责业务逻辑和AI服务调用，包括课程管理模块、知识图谱管理模块、内容生成模块、语音功能模块等。我还设计了模块之间的依赖关系，确保模块的耦合度尽可能低。

在接口设计章节，我详细定义了前后端之间的API接口规范，包括请求方法、请求路径、请求参数、响应格式、错误码等。我使用OpenAPI规范（Swagger）来描述API接口，这样不仅能够生成清晰的API文档，还能够自动生成前端调用的代码骨架。

在数据设计章节，我详细描述了数据库表结构、ER图、数据流转图等。我还设计了缓存策略，对于一些频繁访问且变化不大的数据（如预置课程、知识点关系等），使用内存缓存来减少数据库查询次数，提高系统响应速度。

在安全设计章节，我设计了用户身份验证和授权机制（虽然MVP阶段暂不支持多用户，但预留了扩展接口）、输入验证和防注入攻击措施、API访问频率限制等安全防护措施。

下午我开始搭建项目框架。首先，我创建了项目的目录结构，包括backend和frontend两个主要目录。在backend目录中，我按照FastAPI的最佳实践创建了app目录，包含api、models、schemas、services、core等子目录。然后我初始化了FastAPI项目，安装了依赖包（fastapi、uvicorn、sqlalchemy、pydantic、python-dotenv等）。同时，我配置了alembic用于数据库迁移管理，创建了初始的迁移脚本。

**完成情况**：
- 完成概要设计文档（约8000字）
- 创建项目的目录结构
- 初始化FastAPI后端项目
- 配置数据库迁移工具

**明日计划**：
- 完成前端项目的初始化
- 配置前端开发环境
- 开始实现后端的数据模型

---

### 2026年6月12日（星期五） - 前端初始化与数据模型实现

**工作内容**：
今天上午我完成了前端项目的初始化。我使用Vite创建了Vue 3 + TypeScript项目，安装了依赖包（vue、vue-router、axios、pinia、element-plus等）。然后，我配置了开发服务器代理，将API请求代理到后端服务器，解决前后端分离开发时的跨域问题。同时，我创建了前端的基本目录结构，包括views（页面视图）、components（可复用组件）、services（API调用服务）、types（TypeScript类型定义）、router（路由配置）、store（状态管理）等目录。

在views目录中，我创建了以下页面的骨架：CourseListView（课程列表页）、CourseDetailView（课程详情页）、KnowledgeGraphView（知识图谱页）、CoursePlayerView（课程播放页）等。在components目录中，我规划了可复用组件，如AppHeader（应用头部）、KnowledgeGraph（知识图谱可视化组件）、CourseOutline（课程大纲组件）等。

下午我开始实现后端的数据模型。按照数据库设计，我使用SQLAlchemy ORM定义了6个实体类：Course、KnowledgeNode、KnowledgeRelation、Outline、GeneratedContent、LearningRecord。每个实体类都包含了对应的表名、字段定义、关系定义。我还定义了Pydantic模型（schemas），用于请求和响应的数据验证和序列化。

同时，我配置了数据库连接方式，使用SQLite作为开发数据库。我创建了config.yaml配置文件，存储数据库连接字符串、API密钥、第三方服务的配置参数等。我还实现了配置的加载逻辑，支持从环境变量覆盖配置文件中的参数，便于在不同环境中部署。

**完成情况**：
- 完成前端项目初始化和目录结构创建
- 创建前端页面视图和组件的骨架
- 实现后端数据模型（6个SQLAlchemy实体类）
- 定义Pydantic模型（schemas）
- 配置前后端开发环境

**本周总结**：
本周我完成了系统的概要设计和项目框架搭建。通过详细的技术调研和方案比较，我明确了技术栈和架构方案。同时，我创建了规范的项目目录结构，初始化了前后端项目，为后续的详细设计和编码实现奠定了坚实的基础。虽然作为单人开发项目，工作量较大，但通过合理的规划和高效的技术选型，我相信能够按时完成项目目标。

**下周计划**：
- 开始详细设计和编码实现
- 优先实现知识图谱管理模块（包括后端API和前端组件）
- 实现数据库的迁移脚本和初始数据

---

## 第三阶段：详细设计与编码实现（6月14日-6月18日）

### 2026年6月14日（星期日） - 知识图谱管理模块实现（一）

**工作内容**：
今天我利用周末时间开始实现知识图谱管理模块。作为项目的核心基础设施数据层，知识图谱管理模块需要支持知识图谱的构建、存储、可视化展示与编辑。我计划用两天时间完成这个模块的开发。

上午我实现了后端的知识图谱API接口。首先，我创建了knowledge.py路由文件，定义了以下接口：
- `POST /api/knowledge/nodes`：创建知识点。该接口接收知识点的名称、描述、类型等参数，创建新的知识点节点，并返回节点ID。
- `GET /api/knowledge/nodes`：查询知识点列表。该接口支持分页、过滤（按课程、按类型）、搜索（按名称模糊匹配）等查询条件。
- `POST /api/knowledge/relations`：创建知识点关系。该接口接收源节点ID、目标节点ID、关系类型等参数，创建新的知识点关系，并建立节点之间的关联。
- `GET /api/knowledge/graph`：获取知识图谱数据。该接口返回指定课程的所有知识点节点和关系，数据格式适配前端可视化库的要求。

我使用SQLAlchemy ORM实现了知识点和关系的CRUD操作。在查询知识点列表时，我使用了分页查询和过滤条件组合，确保查询的灵活性和效率。在创建知识点关系时，我添加了重复关系检查，避免创建相同的冗余关系。

同时，我定义了Pydantic模型用于请求和响应的数据验证。我为知识点和关系分别创建了Create、Update、Response等schema，确保数据的完整性和一致性。

**完成情况**：
- 实现知识图谱管理模块后端API（4个接口）
- 实现知识点和关系的CRUD操作（包括分页、过滤、搜索）
- 定义Pydantic数据模型（Create、Update、Response）
- 添加数据验证和错误处理

**明日计划**：
- 继续实现知识图谱的可视化数据接口
- 实现知识图谱的编辑功能（修改、删除）
- 开始前端知识图谱组件开发

---

### 2026年6月15日（星期一） - 知识图谱管理模块实现（二）

**工作内容**：
今天我继续实现知识图谱管理模块。上午我完成了知识图谱的可视化数据接口。该接口需要将数据库中的知识点和关系转换为前端可视化库所需的格式。我选择了ECharts作为知识图谱的可视化库，因为它功能强大、文档完善、社区活跃。

我设计了知识图谱数据的转换逻辑：将知识点节点转换为ECharts的节点数据（包含id、name、symbolSize、category等属性），将知识点关系转换为ECharts的边数据（包含source、target、name等属性）。同时，我支持按节点类型设置不同的颜色和图标，以便在可视化界面中区分不同类型的知识点。

我还实现了知识图谱的编辑功能，支持添加、删除、修改知识点和关系。在删除知识点时，我使用了级联删除策略，自动删除与该节点相关的所有关系，以保持数据的一致性。在修改知识点关系时，我添加了关系类型的更新逻辑，支持修改关系的类型和安全。

下午我开始开发前端的知识图谱组件。我使用Vue 3 + TypeScript开发了KnowledgeGraph.vue组件，集成了ECharts来实现知识图谱的可视化展示。我实现了以下交互功能：
1. **节点拖拽**：用户可以通过鼠标拖拽节点，调整节点的位置。
2. **缩放和平移**：用户可以通过鼠标滚轮缩放知识图谱，通过鼠标拖拽画布平移视图。
3. **点击查看详情**：用户点击节点时，显示该知识点的详细信息（名称、描述、类型等）。
4. **关系高亮**：当用户鼠标悬停在节点上时，高亮显示与该节点相关的所有边。

我还实现了知识图谱的编辑界面，包括添加节点表单、添加关系表单、编辑节点对话框、删除确认对话框等。通过这些界面，用户可以直接在可视化界面中完成知识图谱的构建和编辑。

**完成情况**：
- 完成知识图谱可视化数据接口（适配ECharts格式）
- 实现知识图谱编辑功能（添加、删除、修改节点和关系）
- 开发前端KnowledgeGraph.vue组件
- 实现知识图谱的可视化展示和交互功能

**明日计划**：
- 完成知识图谱模块的前端开发
- 实现知识图谱的搜索和过滤功能
- 完成知识图谱模块的集成测试

---

### 2026年6月16日（星期二） - 知识图谱模块完成与智能课堂生成设计

**工作内容**：
今天上午我完成了知识图谱管理模块的前端开发。我实现了知识图谱的完整交互流程：查看图谱、添加节点、添加关系、编辑节点、删除节点等。用户可以通过工具栏按钮触发相应的操作，操作结果会实时反映在可视化界面中。

我还添加了知识图谱的搜索和过滤功能。用户可以在搜索框中输入关键词，系统会高亮显示匹配的知识点节点。用户还可以通过下拉菜单按节点类型进行过滤，只显示特定类型的知识点。这些功能方便用户快速定位和管理知识点。

下午我完成了知识图谱模块的集成测试。我编写了测试用例，测试了所有API接口的功能和边界情况。同时，我测试了前端组件的交互流程和数据处理逻辑。测试结果表明，知识图谱管理模块功能完整、运行稳定，能够满足需求规格说明书的要求。

在完成知识图谱模块后，我开始了智能课堂生成模块的设计。该模块是平台的核心流程编排模块，负责将用户输入的学习主题转化为完整的结构化课程。我设计了课程生成的流程：
1. **接收用户主题输入**：用户在前端输入学习主题（如"Python编程入门"），系统接收并验证输入。
2. **调用LLM生成课程大纲**：系统将用户主题和组织提示词（Prompt）发送给LLM，请求生成课程大纲。大纲包含多个条目，每个条目包含标题和简要描述。
3. **解析大纲数据**：系统解析LLM返回的文本，提取大纲条目，并存储到数据库中。
4. **为每个大纲条目编排教学场景**：系统根据大纲条目的内容，自动选择合适的学习场景（如讲解、讨论、测验、实验等），并生成相应的教学内容。
5. **返回生成的课程数据**：系统将生成的课程数据（包括大纲、教学场景、教学内容等）返回给前端，供用户查看和学习。

**完成情况**：
- 完成知识图谱管理模块前端开发（包括搜索、过滤功能）
- 完成知识图谱模块的集成测试
- 完成智能课堂生成模块的设计
- 设计课程生成的流程和数据结构

**明日计划**：
- 开始实现智能课堂生成模块后端
- 集成LLM服务（DeepSeek API）
- 设计Prompt模板

---

### 2026年6月17日（星期三） - 智能课堂生成模块实现（一）

**工作内容**：
今天我开始实现智能课堂生成模块。上午我实现了LLM服务的集成。我创建了llm_service.py服务类，封装了与DeepSeek API的通信逻辑。我使用httpx库发送HTTP请求，处理了API的认证、请求参数构建、响应解析、错误处理等细节。

我设计了Prompt模板，用于指导LLM生成结构化的课程大纲。Prompt模板包含了以下要素：
1. **角色定义**：告诉LLM它是一位经验丰富的教师，擅长将复杂的知识分解为易于理解的课程。
2. **任务描述**：明确要求LLM根据用户提供的学习主题，生成一个结构化的课程大纲。
3. **输出格式**：指定LLM的输出格式为JSON，包含课程标题、课程描述、大纲条目列表（每个条目包含标题和描述）。
4. **示例**：提供一个示例，展示期望的输出格式和内容质量。

通过调整Prompt模板，我能够控制LLM生成的内容质量和格式一致性。我还实现了LLM服务的抽象层，定义了LLMProvider接口，支持多个LLM提供商的切换（DeepSeek、通义千问等）。这样，当主服务提供商不可用时，系统可以自动切换到备用服务，提高系统的可用性。

下午我实现了课程大纲的生成接口（`POST /api/courses/generate-outline`）。该接口接收用户的学习主题，调用LLM服务生成课程大纲，解析LLM返回的数据，并将结果存储到数据库中。我还添加了错误处理和重试逻辑，当LLM服务调用失败时，系统会自动重试3次，如果仍然失败，则返回错误信息。

同时，我实现了课程的CRUD接口，支持课程的创建、查询、更新和删除。在创建课程时，我支持从生成的大纲直接创建课程，也支持用户手动创建和编辑课程。我还实现了课程列表的分页查询和按名称搜索功能。

**完成情况**：
- 实现LLM服务集成（DeepSeek API）
- 设计Prompt模板，控制生成内容的质量和格式
- 实现课程大纲生成接口
- 实现LLM服务的抽象层和Fallback机制
- 实现课程的CRUD接口

**明日计划**：
- 继续实现智能课堂生成模块
- 实现教学场景的自动编排功能
- 开始多模态内容生成引擎的设计

---

### 2026年6月18日（星期四） - 智能课堂生成模块完成与多模态引擎设计

**工作内容**：
今天上午我完成了智能课堂生成模块的实现。我实现了教学场景的自动编排功能，根据课程大纲的每个条目自动生成相应的教学场景。我定义了以下教学场景类型：
1. **讲解（lecture）**：AI教师对知识点进行详细讲解，适合概念性和理论性的内容。
2. **讨论（discussion）**：AI教师和AI同学进行讨论，从不同角度探讨问题，适合争议性和开放性的话题。
3. **测验（quiz）**：生成测验题目，检验学生的学习效果，适合所有类型的内容。
4. **实验（experiment）**：生成互动式模拟实验，让学生通过操作加深理解，适合实践性强的内容。

系统根据大纲条目的关键词和内容，自动选择最合适的教学场景类型。例如，如果条目包含"概念"、"定义"、"原理"等关键词，则选择"讲解"场景；如果条目包含"讨论"、"分析"、"比较"等关键词，则选择"讨论"场景。

我还实现了课程结构的返回接口，支持前端展示完整的课程结构，包括课程信息、大纲条目、教学场景等。前端可以根据课程结构，逐步展示课程内容，提供沉浸式的学习体验。

下午我开始了多模态教学内容生成引擎的设计。该模块是平台的内容生产模块，需要基于编排的教学场景，自动生成六种以上模态的教学内容：沉浸式文本、测验题目、PPT与旁白、音频课程、思维导图、互动式教材。我设计了内容生成的流水线架构，支持并行生成多种模态的内容，以提高生成效率。

我为每种模态设计了生成逻辑和输出格式：
1. **沉浸式文本**：调用LLM生成适合学生阅读的、语言生动的教学文本。
2. **测验题目**：调用LLM生成多种题型的测验题目（单选题、多选题、判断题、填空题等）。
3. **PPT与旁白**：使用python-pptx生成PPTX文件，调用TTS服务生成语音旁白。
4. **音频课程**：将课程内容转换为连续的音频课程。
5. **思维导图**：调用LLM提取关键知识点和关系，生成结构化的思维导图数据。
6. **互动式教材**：生成包含交互元素的HTML页面。

**完成情况**：
- 完成智能课堂生成模块实现
- 实现教学场景的自动编排功能（4种场景类型）
- 实现课程结构的返回接口
- 完成多模态教学内容生成引擎的设计
- 设计内容生成的流水线架构

**本周总结**：
本周我完成了知识图谱管理模块和智能课堂生成模块的实现。通过这两周的开发，我深入理解了全栈开发的流程和技术细节。虽然单人开发的工作量较大，但通过合理的时间管理和高效的开发工具，我能够按时完成开发任务。接下来，我将继续实现多模态教学内容生成引擎，这是项目中最复杂、最具挑战性的模块。

**下周计划**：
- 实现多模态教学内容生成引擎（6种模态）
- 集成TTS服务，实现语音合成功能
- 开始语音功能集成的设计

---

## 第四阶段：核心功能实现与集成测试（6月22日-6月26日）

### 2026年6月22日（星期一） - 多模态内容生成引擎实现（一）

**工作内容**：
今天我利用周末时间开始实现多模态教学内容生成引擎。这是项目中最复杂、工作量最大的模块，我计划用三天时间完成这个模块的开发。

上午我实现了沉浸式文本生成功能。我创建了text_generator.py生成器类，调用LLM服务根据课程大纲生成适合学生阅读的沉浸式教学内容。我设计了专门的Prompt模板，要求LLM生成语言生动、通俗易懂、结构清晰的教学文本。生成的文本包含引言、主体内容、总结等部分，并支持插入示例、类比、图示说明等辅助理解的元素。

同时，我实现了测验题目生成功能。我创建了quiz_generator.py生成器类，支持生成单选题、多选题、判断题、填空题等多种题型。我设计了题型模板和评分逻辑，确保生成的题目质量高、答案准确。我还实现了题目的难度控制，支持生成简单、中等、困难三个难度级别的题目，以满足不同学习水平学生的需求。

下午我实现了PPT与旁白生成功能。我创建了ppt_generator.py生成器类，使用python-pptx库生成PPTX文件。我设计了PPT的模板和布局，每张幻灯片包含标题、内容、备注三个部分。标题简明扼要，内容条理清晰，备注包含教师讲解的提示信息。

生成PPT后，我调用TTS服务为每张幻灯片生成语音旁白。我将幻灯片的标题和内容合并为旁白文本，发送给TTS服务，获取生成的音频文件，并将音频文件与PPTX文件关联存储。这样，当用户播放PPT时，可以同步播放语音旁白，实现沉浸式的学习体验。

**完成情况**：
- 实现沉浸式文本生成功能（调用LLM，生成生动的教学文本）
- 实现测验题目生成功能（支持4种题型，3个难度级别）
- 实现PPT与旁白生成功能（使用python-pptx生成PPTX，调用TTS生成旁白）
- 设计内容生成的Prompt模板

**明日计划**：
- 继续实现多模态内容生成引擎
- 实现音频课程生成功能
- 实现思维导图生成功能

---

### 2026年6月23日（星期二） - 多模态内容生成引擎实现（二）

**工作内容**：
今天我继续实现多模态教学内容生成引擎。上午我实现了音频课程生成功能。我创建了audio_generator.py生成器类，将整个课程的文本内容包括旁白转换为连续的音频课程。

我使用了Edge TTS作为主TTS服务，阿里云TTS作为备用服务，确保音频生成的稳定性。在实现过程中，我遇到了一些问题：Edge TTS是免费服务，但有时响应较慢；阿里云TTS响应快，但是付费服务。为了解决这个问题，我实现了TTS服务的Fallback机制，当主服务响应超时或失败时，自动切换到备用服务。

我还实现了音频文件的拼接功能。由于课程文本内容较长，TTS服务通常限制单次请求的文本长度。因此，我将课程文本分段发送给TTS服务，获取多个音频片段，然后使用pydub库将音频片段拼接为一个完整的音频课程。我还支持在音频中插入背景音乐和提示音，以增强听觉体验。

下午我实现了思维导图生成功能。我创建了mindmap_generator.py生成器类，调用LLM服务根据课程内容提取关键知识点和关系，生成结构化的思维导图数据。我设计了思维导图的JSON格式，包含中心主题、分支节点、节点关系等要素。

生成思维导图数据后，我将其转换为前端可视化库（如ECharts、D3.js）所需的格式，并提供API接口供前端调用。前端可以使用可视化库将思维导图渲染为交互式的网页，用户可以通过鼠标拖拽、缩放等操作浏览思维导图。

我还实现了互动式教材生成功能的框架。我创建了interactive_generator.py生成器类，支持生成包含交互元素（如可点击的图表、模拟实验、拖拽练习等）的HTML页面。虽然MVP阶段暂不支持复杂的互动功能，但我设计了可扩展的架构，便于后续添加更多的互动元素。

**完成情况**：
- 实现音频课程生成功能（使用Edge TTS和阿里云TTS，支持Fallback）
- 实现音频片段的拼接和后期处理
- 实现思维导图生成功能（调用LLM提取知识点，生成结构化数据）
- 实现互动式教材生成框架（支持生成包含交互元素的HTML页面）

**明日计划**：
- 完成多模态教学内容生成引擎
- 实现内容生成的流水线调度功能
- 开始语音功能集成的实现

---

### 2026年6月24日（星期三） - 多模态引擎完成与语音功能集成（一）

**工作内容**：
今天上午我完成了多模态教学内容生成引擎的实现。我实现了内容生成的流水线调度功能，支持根据用户需求选择性地生成特定模态的内容。用户可以在前端选择需要生成的内容模态（如只生成PPT和音频，不生成思维导图），系统会根据用户的选择调用相应的生成器，提高生成效率。

我还添加了内容生成的进度跟踪功能。由于内容生成可能需要较长时间（特别是PPT和音频生成），我使用WebSocket实现了实时进度推送。后端在生成过程中定期发送进度消息（如"正在生成PPT第3页..."、"正在合成音频..."），前端接收消息并更新进度条，让用户了解生成进度，提升用户体验。

下午我开始了语音功能集成的实现。首先，我实现了TTS语音合成功能。我创建了tts_service.py服务类，调用Edge TTS或阿里云TTS服务将文本转换为语音。我设计了TTS服务的抽象层，定义了TTSService接口，支持多个TTS提供商的切换。用户可以在前端选择喜欢的音色（如男声、女声、童声等），系统会根据用户的选择调用相应的TTS服务。

我还实现了语音文件的存储和管理功能。生成的语音文件存储在backend/data/audio/目录下，按课程ID组织。我创建了AudioFile模型，记录语音文件的路径、时长、格式、生成时间等元数据。我还实现了语音文件的缓存机制，对于相同的文本和音色，系统会直接返回缓存的语音文件，避免重复生成，节省时间和成本。

**完成情况**：
- 完成多模态教学内容生成引擎（6种模态全部实现）
- 实现内容生成的流水线调度功能（支持选择性生成）
- 实现内容生成的进度跟踪和实时推送（使用WebSocket）
- 实现TTS语音合成功能（支持多个TTS提供商，支持音色选择）
- 实现语音文件的存储、管理和缓存机制

**明日计划**：
- 实现ASR语音识别功能
- 实现语音交互的完整流程
- 开始系统集成测试

---

### 2026年6月25日（星期四） - 语音功能集成完成与系统测试

**工作内容**：
今天上午我完成了语音功能集成的实现。我实现了ASR语音识别功能，调用阿里云或腾讯云ASR服务将语音转换为文本。由于ASR服务需要实时流式传输，我使用了WebSocket协议与ASR服务进行通信。

我创建了asr_service.py服务类，封装了与ASR服务的WebSocket通信逻辑。我实现了音频数据的实时采集、分帧、发送，以及识别结果的接收和解析。我还添加了语音活动检测（VAD）功能，自动检测用户开始和结束说话的时间点，避免发送静音数据，节省带宽和提高识别准确率。

同时，我实现了语音交互的完整流程：用户语音输入 → ASR识别 → LLM处理 → TTS合成 → 语音输出。这个流程模拟了真实课堂中师生互动的场景，学生可以通过语音提问，AI教师通过语音回答，创造沉浸式的学习体验。我还实现了语音交互的历史记录功能，将每次交互的输入文本、输出文本、音频文件路径等存储到数据库中，方便用户回顾和复习。

下午我开始了系统集成测试。我按照测试用例，对四大核心功能模块进行了集成测试：
1. **测试智能课堂生成流程**：输入学习主题，检查课程大纲生成是否正确，教学场景编排是否合理。
2. **测试多模态内容生成流程**：选择生成的内容模态，检查生成的内容是否完整、格式是否正确。
3. **测试知识图谱管理功能**：添加、删除、修改知识点和关系，检查知识图谱的可视化展示是否正确。
4. **测试语音功能**：测试TTS语音合成是否流畅、ASR语音识别是否准确、语音交互流程是否顺畅。

在测试过程中，我发现了几个Bug：1）PPT生成时某些特殊字符导致格式错误；2）语音识别在嘈杂环境下的准确率较低；3）知识图谱节点过多时，可视化界面卡顿。我开始分析和修复这些Bug。

**完成情况**：
- 完成语音功能集成（TTS + ASR）
- 实现语音交互的完整流程（输入→识别→处理→合成→输出）
- 实现语音交互的历史记录功能
- 开始系统集成测试
- 发现并修复部分Bug（PPT格式问题、语音识别准确率问题）

**明日计划**：
- 继续系统测试和Bug修复
- 优化系统性能和用户体验
- 准备项目演示和验收材料

---

### 2026年6月26日（星期五） - 系统测试完成与项目验收准备

**工作内容**：
今天上午我继续进行了系统测试和Bug修复。首先，我修复了昨天发现的PPT生成格式问题。我分析了问题原因，发现是因为某些特殊字符（如中文标点、数学公式等）在python-pptx中处理不当。我添加了特殊字符的过滤和转义逻辑，并对PPT的布局和字体设置进行了优化，确保生成的PPT格式正确、美观大方。

然后，我改进了ASR语音识别的准确率。我分析了识别错误的原因，发现主要是因为环境噪音和口音问题。为了解决这个问题，我添加了降噪处理（使用noisereduce库），并优化了语言模型（添加专业术语词典）。经过改进，语音识别的准确率有了显著提升，特别是在安静环境下的识别准确率达到了95%以上。

我还优化了知识图谱可视化界面的性能。当知识点节点过多时（如超过200个节点），ECharts的渲染速度会变慢，导致界面卡顿。为了解决这个问题，我实现了节点的懒加载和聚合显示策略：当节点数量超过阈值时，自动聚合相邻的节点，用户双击聚合节点时才展开显示。这大大提升了可视化界面的响应速度。

下午我完成了所有功能的集成测试，系统运行稳定，能够满足需求规格说明书的要求。然后，我开始准备项目演示和验收材料。我整理了项目文档，包括立项报告、需求规格说明书、概要设计文档、用户手册等。我确保文档的完整性和一致性，删除冗余内容，补充遗漏的细节。

同时，我录制了系统演示视频，展示平台的核心功能：智能课堂生成、多模态内容生成、知识图谱管理、语音交互等。我在演示视频中详细讲解了系统的使用流程和特色功能，突出系统的创新点和优势。我还准备了项目验收报告，总结项目的成果、经验教训、以及后续改进方向。

**完成情况**：
- 完成系统测试和Bug修复（PPT格式、语音识别准确率、知识图谱性能）
- 优化系统性能和用户体验
- 整理项目文档（立项报告、需求规格说明书、概要设计等）
- 录制系统演示视频
- 准备项目验收报告

**本周总结**：
本周我完成了多模态教学内容生成引擎和语音功能集成的实现，同时完成了系统集成测试和Bug修复。作为单人开发的实训项目，这个项目涵盖了前后端开发、数据库设计、AI服务集成、文档编写等多个方面，让我得到了全面的锻炼和提升。虽然开发过程中遇到了很多挑战，但通过查阅资料、反复调试、持续优化，我最终完成了项目的所有功能，达到了预期的目标。

**下一步计划**：
- 项目验收和交付
- 编写项目总结报告
- 规划后续的功能扩展和优化

---

## 项目工作总结

### 完成情况
截至6月26日，智能互动教学平台项目已完成以下工作：
1. **项目立项与需求分析**（6月1日-6月4日）：完成立项报告、需求规格说明书的编制和评审，明确项目目标、功能范围和非功能性需求。
2. **系统设计与技术选型**（6月8日-6月12日）：完成概要设计、数据库设计、技术栈确定，创建项目框架和目录结构。
3. **详细设计与编码实现**（6月14日-6月18日）：完成知识图谱管理模块、智能课堂生成模块、多模态引擎设计，实现后端API和前端组件。
4. **核心功能实现与测试**（6月22日-6月26日）：完成多模态内容生成引擎、语音功能集成、系统集成测试和Bug修复，准备项目验收。

### 工作量统计
- 项目总工作量：25人天（实际用时17个工作日，包含周末加班）
- 完成功能模块：4个核心模块全部完成
- 编写文档：立项报告、需求规格说明书、概要设计文档、技术文档等约20000字
- 编写代码：后端Python代码约5000行，前端Vue/TypeScript代码约3000行

### 技术收获
1. **全栈开发能力**：通过独立完成前后端开发，深入理解了Web应用的完整开发流程，掌握了Vue 3、FastAPI、SQLAlchemy等技术栈的使用。
2. **AI服务集成**：通过集成LLM、TTS、ASR等AI服务，了解了AI API的调用方法和最佳实践，掌握了Prompt工程设计技巧。
3. **数据库设计**：通过设计6张数据库表，掌握了关系型数据库的设计原则和方法，熟悉了SQLAlchemy ORM的使用。
4. **文档编写**：通过编写项目文档，掌握了软件工程中文档的编写规范和方法，提高了技术写作能力。

### 经验教训
1. **技术选型要适合项目**：在选择技术栈时，要考虑项目的需求、团队的能力、开发的时间等因素，选择最适合的技术，而不是最流行或最复杂的技术。
2. **集成测试很重要**：早期进行集成测试可以及时发现模块间的问题，避免问题累积到后期难以修复。
3. **文档要及时更新**：在开发过程中要及时更新设计文档，保持文档与代码的一致性，避免文档过时导致后续维护困难。
4. **single开发要合理规划时间**：单人开发项目容易低估工作量，要合理规划时间，预留足够的缓冲时间应对突发情况。

### 后续工作
1. 项目验收和交付：向指导老师演示系统功能，提交项目文档和代码，完成项目验收。
2. 根据用户反馈进行功能优化：根据指导老师和同学的反馈，优化系统功能和用户体验。
3. 规划下一阶段的工作：添加多用户支持、云端部署、移动端适配等功能，将项目打造成一个完整的、可商业化的产品。

---

